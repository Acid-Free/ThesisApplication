// Class for general configurations used by the system
public class AdvancedConfigurations{
    // Used by numerical digits for storing the accuracy after the decimal point
    public static int accuracy = 3;
    // Used by numerical digits for showing how many numbers to show after the decimal point
    public static int visual_accuracy = 1;
    // Determines the count of features to be shown in the second panel
    public static int maxFeature = 10;
}
